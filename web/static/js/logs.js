import{api}from"./api.js";
let loading=false;const consoleNode=document.querySelector("#log-console"),selector=document.querySelector("#log-job");
const escape=value=>String(value??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
export async function loadLogs(){if(loading||!selector.value)return;loading=true;try{const data=await api.logs(selector.value);consoleNode.innerHTML=data.entries.length?data.entries.map(e=>`<div class="log-line"><span class="log-time">${new Date(e.timestamp).toLocaleTimeString()}</span><span class="log-stream ${e.stream}">${escape(e.stream)}</span><span>${escape(e.message)}</span></div>`).join(""):'<p class="console-empty">Este proceso todavía no ha generado logs.</p>';if(document.querySelector("#auto-scroll").checked)consoleNode.scrollTop=consoleNode.scrollHeight}catch(error){consoleNode.innerHTML=`<p class="console-empty">${escape(error.message)}</p>`}finally{loading=false}}
export function selectLogs(name){selector.value=name;document.querySelector("#logs").scrollIntoView();loadLogs()}
export function setupLogs(){selector.addEventListener("change",loadLogs);document.querySelector("#refresh-logs").addEventListener("click",loadLogs);document.querySelector("#clear-logs").addEventListener("click",()=>{consoleNode.innerHTML='<p class="console-empty">Visualización limpiada. Los archivos no fueron modificados.</p>'});return()=>{if(document.querySelector("#auto-logs").checked)loadLogs()}}
